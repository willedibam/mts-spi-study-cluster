# P90 proof and cross-M,T validation

Active workstream as of 2026-08-24. Facts are marked **verified**. The Pearson development protocol is frozen; this study tests quantitative transfer, not mathematical invariance.

## Baseline exploration, 2026-09-21

Completed focused exploration: [lean notebook](../../notebooks/embeddings/spi_baseline_exploration_260921.ipynb), 21 executed cells/four figures, plus a separate seven-cell/two-figure [Zenodo appendix](../../notebooks/embeddings/spi_baseline_zenodo_260921.ipynb). Repository checkpoint `9ba2094` preserves the preceding work; temporary review images excluded. Protocol `configs/analysis/spi-baseline-exploration-260921.yaml`; runner/builder/plots/audit `scripts/{spi_baseline_exploration,build_spi_baseline_notebooks,plot_spi_baseline_exploration,audit_spi_baseline_exploration}.py`. All p90 reused. Gadi retrieval supplied the remaining 1,120 proof development MPI archives, development raw inputs, 256 Kuramoto MPI archives and 544 CML MPI archives/features. Proof source inventory/retrieval helper: `scripts/prepare_spi_baseline_data.py`; local cache under `data/spi_baseline_exploration_260921`, existing Stage A mirror reused.

Compare two raw Pearson summaries, 289 per-SPI means, seven distribution summaries/SPI, original frozen z-PCA50 and equal-variance combinations. Preserve symmetrized proof edges, development instances 0–9/evaluation instances 10–29, original all-14-class PCA and historical 13-class/five-CML display. New comparisons are retrospective and pool all development sizes; they do not repeat the frozen leave-cell-out confirmation. Standardized/clipped marginal PCA versus preserved centre-only z; fusion balances separate PCA50 blocks before another PCA50. Fixed C=1 logistic, no tuning; hard retrieval excludes gallery rows matching either query M or T. Conditional paired bootstrap groups class–instance across cells.

Verified results: across 14 classes, balanced accuracy for means/distributions/z is .907/.925/.983 and hard mAP is .698/.714/.818. Within five CML regimes, accuracy is .976/.986/.991 and hard mAP is .741/.766/.940. About 98% of the net all-class z–distribution classification gain comes from two phi=.2 VAR classes; do not recast this as nonlinear-mechanism discovery. Brownian-defect is worse with z. The CML z–distribution classification difference is unresolved near ceiling, while the retrieval difference is clear. Distribution+z has all-class mAP .823 but lower accuracy than z; fusion is not uniformly superior. Authoritative exact tables/conditional intervals/per-record outcomes: `results/spi_baseline_exploration_260921/{proof-metrics.csv,proof-paired.csv,proof-individual.csv}`.

Intuition uses a realizable stationary Gaussian VAR with prescribed contemporaneous C and lagged L. Changing average C leaves z unchanged; reversing the L profile preserves both complete marginal distributions but changes z from +1 to −1. This distinguishes compressions of two selected probes, not full-p90 mean insufficiency or nonlinear mechanism. Framing: each system supplies a geometry among fixed scientific dependence measures; common dimensionality alone is not unique because marginal baselines also provide it. Five tests, independent MPI/raw replay, all 2,660 overlaps with the existing mean bank, 144 independent retrieval replays, independent mean/distribution PC1 SVD checks and original-notebook identity checks pass; see `verification.json`. All new results are exploratory. No new p90, cluster compute jobs or changes to original notebooks.

## Verified starting state

- The current pyspi-v3 proof bank has 900 rows: ten classes, `M={8,16,32}`, `T={500,1000,2000}`, instances `0:9`; feature artifact `/scratch/ql44/we2614/spi-spi-direction-v2/proof.npz` has 289 SPIs/111 directed, `41,616` symmetric and `32,079` directional features, schema `803abbe5…`, complete pyspi `3.0.0.r7` provenance, and Pearson SPI-edge aggregation.
- Goal 2 uses Pearson SPI-edge aggregation, matching the existing proof artifact and the established proof construction. A completed Spearman development freeze from job `177252878` is non-authoritative and must not release confirmation; it is retained only as an audit artifact.
- Goal 1 compact JSONs were copied to the ignored local path `results/spi_spi_direction_v2/`; proof/CML-panel/CML-order/Kuramoto SHA-256 are `94cb7eba…`, `c690e6f1…`, `c243b94d…`, `08ccfe81…`. Large feature banks remain on Gadi.
- The existing six-class CML panel cannot be pooled as development evidence: it contains 297 SPIs from `configs/pyspi-v2/benchmarked90_amortized_config.yaml`, lacks config/version provenance, and has a different schema. Its four non-overlapping classes must be recomputed under current p90; the two overlapping classes (`defect-turbulence`, `sti-i`) already exist in the proof bank.
- The four additions are `frozen-chaos`, `brownian-defect`, `fdstc`, and `chaotic-traveling-wave`, yielding 14 classes. Existing dirty notebooks are out of scope and must not be overwritten.

## Generation boundary

- Development additions: `configs/generate/embeddings/cross-mt-cml-development-260824.yaml`, instances `0:9`, 360 rows under gdata. Confirmation: `cross-mt-confirmation-260824.yaml`, instances `10:29`, 2,520 rows under a separate gdata root. Overlapping generator parameters exactly match the original proof config.
- Explicit instance lists preserve the untouched split. Gadi farms are selected by one `(M,T)` cell, use one core/dataset, and emit only `timeseries.npy`, `spi_mpis.npz`, `meta.json` and logs. Confirmation generation must not begin until the common schema and analysis manifest pass development checks and are committed.
- **Verified:** one-row smoke `177231878` completed in 126.5 s with 289 SPIs, exact config/version provenance and no table/heatmap. Development farms `177242178/191/194/198/203/216/227/234/238` covered the nine homogeneous 40-row cells and all exited 0. Initial freeze job `177247167` correctly rejected a Pearson/Spearman mismatch; Spearman freeze `177252878` completed but was superseded before confirmation. Authoritative Pearson freeze `177255809` exited 0.

## Implemented protocol before confirmation

- `configs/analysis/cross-mt-transfer-260824.yaml` freezes the proposal. Primary: `z_sym`; prespecified sensitivities: `z_dir` and development-total-variance-balanced `[z_sym,z_dir]`.
- Each leave-one-`(M,T)`-cell-out fold fits validity filtering, median imputation, centring, PCA and multinomial logistic regression only on instances `0:9` from the other eight cells; evaluation uses instances `10:29` from the held cell.
- Complementary tests: cross-cell retrieval and a stricter gallery where both `M` and `T` differ; per-class prediction of `M`, `T` and joint cell (size leakage); additive class-versus-cell geometry in one development-fitted shared PCA space. Shared PCA/UMAP figures are illustrative, not inferential.
- Three fixed-dimensional raw-data baselines pool channelwise temporal/marginal summaries (50 features), pairwise-dependence distributions (32), or both (82); their scaling/models use development rows only.
- The implementation fixes PCA50, multinomial logistic regression (`C=1`), 1,000 stratified bootstrap replicates and 1,000 held-label permutations. `scripts/freeze_cross_mt_protocol.py` serializes every development-fitted fold/shared model; `analyze_cross_mt_transfer.py` consumes that bundle without refitting. No confirmation label may tune any choice.

## Frozen development state

- **Verified:** the 1,260-row grid is exact and unique across 14 classes, nine `(M,T)` cells and instances `0:9`. Both source artifacts have Pearson metric, schema `803abbe5…`, 289 SPIs/111 directed, 41,616 symmetric and 32,079 directional features, and complete current-v3 provenance.
- The content-addressed manifest is tracked at `results/cross_mt_transfer_260824/development-manifest.json`, SHA-256 `775d0fc37a817cee4e332ee9a468d0dd5b8050f8e5ea8bacbd10c604f6f7f978`. Its status is `development_frozen_confirmation_unseen`; protocol, model-bundle and baseline-cache hashes were independently verified.
- Development filtering retains 21,280–22,567 symmetric, 12,608–13,900 directional and 33,888–36,467 balanced-augmented features across held-cell folds. PCA50 explains 88.8–90.7%, 89.4–91.2% and 88.6–90.5% respectively.
- The updated six-class current-v3 CML development check uses instances `0:5` for fitting and `6:9` for evaluation: balanced accuracy is 0.981 for `z_sym`, 0.981 for `z_dir`, and 0.968 for balanced augmentation. These are development diagnostics, not confirmation evidence.

## Held-out confirmation

- **Verified:** the audited manifest was committed in `8fcb086`. Two-dataset smoke `177256967` (instances `10:11`, `M=8,T=500`) exited 0 with exact current-v3 provenance and no tables/heatmaps.
- The nine 280-row homogeneous confirmation farms were `177257654/658/662/663/667/671/688/694/699`; each used one core per selected dataset. The first eight exited 0. Job `177257699` completed 248/280 rows for `M=32,T=2000` before exiting 1 at 02:10:06; its final validator reported the exact 32 missing indices, while completed artifacts remained intact.
- Recovery `177270330` skipped the 248 complete rows and finished all 32 slow rows (exit 0). The final audit proved an exact unique `14 classes x 9 cells x 20 instances = 2,520` grid, current pyspi/config provenance for every row, raw normalization and no CSV/parquet/heatmap outputs. Pearson reconstruction `177270338` and frozen analysis `177270341` exited 0.
- Primary `z_sym` held-cell balanced accuracy is `.9913` (95% CI `.9877–.9944`, permutation `p=.000999`; worst cell `.9679`). Hard retrieval, where both `M` and `T` differ between query and gallery, has mAP `.8177` (CI `.8142–.8209`, random-ranking expectation `.0812`; worst cell `.7946`).
- Sensitivities: `z_dir` accuracy `.9937`/mAP `.7991`; balanced augmentation `.9798`/`.8196`. Best pooled baselines are `.9766` accuracy (combined) and `.7674` mAP (univariate). SPI–SPI estimates are descriptively higher, but no paired baseline-difference test was frozen, so this is not a formal superiority result.
- Observation size remains encoded: class-conditioned joint-cell accuracy for `z_sym` is `.5790` (chance `.1111`, permutation `p=.000999`), despite class/cell additive geometry eta-squared `.801/.010` and matched-distance ratio `.335`. The defensible conclusion is strong quantitative cross-size transfer with residual size information, not invariance.
- Authoritative compact artifacts are `results/cross_mt_transfer_260824/{development-manifest.json,confirmation-results.json,confirmation-coordinates.npz}`; the 458 MB feature matrix remains at `/scratch/ql44/we2614/spi-spi-cross-mt-260824/confirmation.npz`. Their recorded SHA-256 bindings were verified. The user-facing proof is `notebooks/embeddings/proof_p90_260824.ipynb`; it presents the 14-class PCA/UMAP result first and retains cross-size validation as an appendix. Frozen artifact/config paths retain `cross_mt_transfer_260824` because those names are provenance-bound.
- Verified local ignored replicas now exist under `data/proof_p90_260824/features/`: the 168 MB base development bank (`a7cdf5d9…`), 61 MB CML additions (`f2ee16f8…`) and 458 MB confirmation bank (`847a7b4b…`), matching the recorded Gadi hashes. `proof_p90_geometry_comparison.ipynb` finds centre-only PCA50 best among the tested scalings by independent-confirmation mAP (`.839`); standard is `.827`, robust `.773`, and historical transductive preprocessing `.831` while dropping 45 development and 80 confirmation rows.
- The 2,520 confirmation `timeseries.npy` files are mirrored without MPI outputs under `data/proof_p90_260824/raw/confirmation/` (213 MiB; zero missing), enabling the proof notebook's opt-in live point-to-MTS browser.
