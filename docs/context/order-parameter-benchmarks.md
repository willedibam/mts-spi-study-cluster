# Dynamical order-coordinate benchmarks

## Question and representation policy

2026-09-21 catalogue-matched baseline follow-up: [lean baseline notebook](../../notebooks/embeddings/spi_baseline_exploration_260921.ipynb), runner `scripts/spi_baseline_exploration.py`, exact outputs/provenance/audit `results/spi_baseline_exploration_260921/`. Reused existing 256 Kuramoto and 72+544 CML MPI archives; no p90 or physics rerun. Fit mean-SPI, seven-summary/SPI and mean+z PC1 without control/Q on original development rows (Kuramoto 128; CML 36); evaluate the original 128 held and 544 confirmation rows, with no common-eligibility losses. Absolute rho for original q / mean PC1 / distribution PC1 / mean+z is .826/.994/.994/.993 in Kuramoto and .883/.973/.973/.954 in CML. A matched-standardization z-PC1 sensitivity added after primary results yields .964/.941, still below means. This demonstrates pipeline dependence, not absence of information in full z.

Independent SVD reproduces the new mean/distribution scores within 1.34e-14; original q reconstruction passes. Paired seed-bootstrap intervals are conditional, retrospective and unadjusted. Localization is distinct from rank association: CML q/distribution/fusion share Q's steepest interval [3.86212,3.86306], while mean PC1/mean absolute r peak in adjacent [3.86006,3.86212]. The original Kuramoto dominance-screen failure remains unchanged. Display signs use development Q only for orientation; fitting/component selection remains target-blind. See cross-M/T context for the associated proof/intuition study and notebook reproduction.

2026-09-17 simple-baseline audit: user requests a separate copy, `notebooks/inference/order-parameter-benchmarks-baselines.ipynb`, leaving lean/original notebooks unchanged. Reproduction: `scripts/order_parameter_simple_baselines.py`, `scripts/build_order_parameter_baseline_notebook.py`, plotting helper `scripts/plot_order_parameter_baselines.py`; data/metrics/provenance under `data/order_parameter/simple_baselines_260917`. Exact stored MTS inputs retrieved (timeseries/meta only) for old Kuramoto/SL/MH/Kaneko banks; new finite/CML banks already local, missing pilot M/T arrays reconstructed as verified nested prefixes. All overlapping existing baseline columns reproduce. Zero-lag Pearson MPI uses np.corrcoef; compare mean off-diagonal signed r and mean abs(r), entropy, Hilbert coherence, mean channel SD and leading correlation-eigenvalue fraction. No new simulation/p90/model fit. Headline identical-row |rho(q,Q)| / mean-r / mean-abs-r: partial Kuramoto .956/.957/.975; full .826/.999/.998; SL broad M32T1000 .968/.988/.986; SL fine .886/.968/.915; MH M32 .931/.054/.924; Kaneko band .809/.357/.944, entropy .804/.382/.951; 2D confirmation .883/.889/.966; Rössler .848/.730/.849. MH channel SD .962, entropy .955; CML entropy .972. Thus current q results demonstrate sensitivity, not advantage over simple summaries; no inference that full z contains zero additional information. Mean-abs-r paired seed-bootstrap difference exceeds zero except MH/Rössler (intervals include zero); retrospective unadjusted comparisons, no newly confirmed superiority claim. Headline cohorts differ from older pooled-M numbers. Failed patch/L6 remain failures, patch comparisons use only same14 eligible rows. Curves retain physical Q units and display-standardize q/baselines without Q, using shared constants across each M/T/layout family; original q values/scatters preserved. Metrics include individual-record, control-mean, within-control residual rho and 2,000 paired seed-cluster bootstrap differences; per-M/T metrics separate. Raw residual q sign is arbitrary. Five focused tests pass; original lean SHA256 b6da4ef0a6707533c213dc680840a8c13c328fb8e2cab320b6adb87a42fdb608. No new monitor or cluster jobs.

2026-09-16 lean presentation COMPLETE, including full Kuramoto: `notebooks/inference/order-parameter-benchmarks-lean.ipynb`, builder `scripts/build_lean_order_parameter_notebook.py`,38 executed cells/32 figures. Each system follows System/equation → Params → separate phase/order-paper locators → sweep/M/T → Q/meaning → Results. Headline and available M/T plots precede composites (small-L CML excepted), enforced by tests. Kuramoto/Rössler have no multi-T banks. New Kuramoto headline/scatter and composite are under `notebooks/inference/figures/lean/kuramoto-full-*`; model and gates unchanged, exploratory association distinguished from dominance-screen failure. Original comparison SHA256 unchanged: `29e94f59e27d5f3275900a6db0dfebea8912320a5b69f949f79f90c92889134e`. Fixed illustrative snapshots at `data/order_parameter/lean_snapshots_260916`, prepared by `scripts/prepare_lean_benchmark_snapshots.py`; helper `scripts/lean_benchmark_figures.py`. MH/Kaneko fields match sensors exactly; SL trace is abs(Z), Rössler display clips +/-3 without changing inputs. Prior SL wording correction remains unedited as requested. No active jobs/reminder for this run.

Kuramoto M=N32/T1000 COMPLETE, collected and independently numerically checked. Production179111658 finished38m20, audit256/256 complete/no failures; analysis179111699 finished1m48. All289 SPIs attempted,22998 retained pairs,255/256 eligible and128/128 held; coverage passes. EVR.277640/.187816 gives ratio1.47825<1.5; seed-stability min cosine.896872 passes. Held rho=.826114,95%CI[.706074,.941258], control-mean=.997059; within-control CI[.200789,.854391]; raw meanabs-correlation=.997803. q/Q steepest intervals adjacent [1.0575,1.0875]/[1.0225,1.0575]. **Interpretation clarified:** 1.5 is a target-blind workflow heuristic inherited from CML protocol/source d619b02 and finite pipeline936dfb7, not a physical requirement, calibrated significance test or necessary condition for q/Q association. Keep the unchanged screen verdict, but do not equate its failure with absence of tracking/information; results are exploratory held-seed evidence, not fresh frozen confirmation. Independent float64 preprocessing/SVD/leave-seed reconstruction reproduces model, q to1.91e-14 and rho/bootstrap CI; archive/manifest/source/model hashes and row/target identities pass. No raw-MPI replay in this audit. Excluded development row kappa1.525/seed260916002 has27.26% selected missingness; original pipeline imputes during development fitting before downstream row exclusion. Local model/features hashes match remote bdd6a455…/aa3881ea…. No refit, cutoff change or new farm.

Kuramoto reproduction: `scripts/kuramoto_full_observation_260916.py`;16 kappa .625–1.65,16 Gaussian populations/seeds260916001–016(first8development,last8held), paired frequencies/starts across kappa; RK4dt.02/sample.1/burn200/disjointfuture10000. All physical inputs pass; mean futureR .263372→.864012, max reference halfdifference.017366. Local root (including analysis) `data/order_parameter/kuramoto_full_observation_260916/primary`; remote `/scratch/ql44/we2614/mts-spi-data/order_parameter/kuramoto_full_observation_260916/primary`; archiveSHA256 `6ba99c634c12d5e21bc6f78016720fff6df0d920a2138049949bec2ecb5dd65a`. Isolated branch `codex/kuramoto-full-260916`, commit `a0c8aefcaa6ed723724552c7b828ca5af6e52c5c`, Gadi source `/scratch/ql44/we2614/kuramoto-full-source-a0c8aef`. Smoke179111598 finished32m27; production used256workers/576reservedCPUs/2256GB,2400s per-task limit; unchanged generic analysis. All cosine channels means full oscillator coverage, not both phase quadratures. Scratch inode headroom was~2000 before outputs: recheck before further farms.

Focused post-closure follow-up COMPLETE: unchanged N32 TASEP model applied to existing N64 cached features, without any new SPIs/refit/rescaling. All168 rows valid with zero selected missingness; held84rho=.782363 [.726445,.867094], control-mean=.922078. Original model arrays identical; independent direct projection agrees2.22e-16; eight generic/cache tests pass. This is retrospective size transfer, not fresh confirmation: N64native outcomes were known and physical N changes with M. Evidence `data/order_parameter/finite_regime_260915/tasep/N64/frozen-N32-analysis`, reproduction `scripts/analyze_finite_cached_transfer.py`; existing comparison notebook records the result. The finite report contains the pre-calculation gate/provenance protocol. [Candidate evaluation](../research/order-parameter-benchmarks/candidate-evaluation-260910.md) now includes larger-M cost scoping, no-cosmetic-grid-rerun guidance, and Chua/Lorenz intermittency as literature-backed untried three-state contenders, with inertial Kuramoto a redundant reserve. No new-family simulation, M>64 farm, or reminder was launched. Pair-count estimates atT1000 from TASEP64 are2.47h/M100,9.93h/M200,16.30h/M256,65.31h/M512; not measured guarantees or RAM predictions. Existing smallCML L16=N256 still has basin/periodicity problems, so more feasible observed M does not automatically repair its physics.

COMPLETE finite full-observation follow-up, 06:10 AEST 2026-09-15: [final outcomes, sources and reproduction ledger](../research/order-parameter-benchmarks/finite-full-observation-260915.md). Rössler full-six-state and TASEP fullN32 frozen32-fresh-seed confirmations each retain672/672 rows with unchanged models/sign/gates. Rössler rho=.848394 [.835212,.861571], control-mean=.922078, q/Q steepest interval both .02625–.0275; raw-correlation baseline tied. TASEP32 rho=.745693 [.704812,.786887], control-mean=.967532, q/Q intervals adjacent .200–.205 / .195–.200; sampled-density baseline stronger .827989. Both within-control CIs include zero. Independent audits reproduce seals/arrays/source/raw identities, eligibility, q to3.02e-14/1.80e-14 and bootstrap statistics; three original T32masters verify event-weighted Q rather than diagnostic-trace means. Final native TASEP64 pilot also passes168/168, zero selected missingness,14,348features, EVR=.544121/cosmin=.986998; held rho=.812980 [.778948,.863851], control-mean=.942857. Independent development-only refit/q audit agrees7.55e-15. N64uses its own model, not frozen N32→N64 transfer; no extra confirmation warranted. Supports across-control inference/tracking of established regime quantities, not exact thresholds, within-regime recovery, numerical calibration, unseen controls or superiority. L96/HR valid geometry but weak rho=.331/.161 remain concise negatives; HR window-Q baseline only68/84held rows. Notebook59cells executes, final figures inspected, accepted CML unchanged. All finite jobs and agents finished; **complete-finite-regime-overnight-pilots deleted with app confirmation**. No new arm/reminder or CML monitor remains; HCP untouched. Immutable p90 c5a079d forR/T/L96,8334c25 forHR;6e71c45 Rconfirmation physics/analysis,4990d54 Tconfirmation physics/analysis. Banks gdata/local `finite_regime_260915`, except T32confirmation `/scratch/ql44/we2614/finite-regime-tasep32-confirmation-260915` mirrored locally at `data/order_parameter/finite_regime_260915/tasep/confirmation-N32`. Costly MPIs remain remotely; manifests/raw observations/features/models/results are local. FHN excluded from this run on verified source-sign/scale ambiguity, not silently repaired or treated as a q failure.

COMPLETE small CML, 2026-09-15: [final outcome and evidence](../research/order-parameter-benchmarks/cml2d-full-observation-260914.md). L6 production178982490/report178982491 exit0; all680 rows valid, but only40 retained features and PC1 stability fails (EVR.976535, leave-seed cosine.223817). Two non-period-four development observations from seed260914007 drive the direction. Unchanged L256 transfer fails all680 rows (45.60–99.95% missing selected features); L8 previously stopped on50 constant-channel records. Independent audit reproduces q to3.55e-15, SVD/stability, gates, identities and unchanged transfer arrays. Notebook executes and displays explicitly descriptive failed-gate q; accepted large-L figure hashes unchanged. CML monitor deleted with app confirmation. Structural620-case/1360-record audit verified: stable periodic attractors dominateL6; no clean M=N<=64 workaround found. L24/32/64 all120random-start cases have positive late Lyapunov estimates, but tiled periodic stripes remain stable throughL24, so no universal minimum-size theorem. No further CML jobs/reminder remain.

COMPLETE CML2D independent confirmation: [final report](../research/order-parameter-benchmarks/cml2d-confirmation-results-260911.md), [sealed protocol](../research/order-parameter-benchmarks/cml2d-confirmation-protocol-260911.md), [execution ledger](../research/order-parameter-benchmarks/cml2d-results-260911.md). 32 fresh seed clusters260911101–132,17r(old9+8midpoints),544 masters L256/N65536,burn200k/obs2k/future1m;544 primaryM32T1000+864 paired old-grid M16T500/M16T1000/M32T500. Original frozen q unchanged. ALL1408 pass prospective row/coverage gates: zero exclusions/missing selected features,32/cell. Main rho=.883018[.867676,.898475],controlmean1.0; new-midpoint-only rho=.879546[.853120,.900824]; old-grid primary=.882634. q/Q steepest interval same3.86212–3.86306. Within-r futureQ=.07846[-.0360,.1695] weak; rawmeanabsr stronger.96637,sampledQ.89422. Secondary rhos .83730/.83688/.89375; paired q agreement .86738/.91137/.92873, mean shifts -.12469/-.02312/-.12104 originalSD. M16T500 localisation shifts one original-grid interval; no exact M,T invariance or monotonic T benefit claimed. Allcomputations/reports/audit/archive completed; no new experiment/reminder pending. Independent audit1408 member IDs,864 prefixes/sharedtargets,allmodelarrays/coreidentity,qreconstruction2.22e-16,8MPIreplays; local rawhashes/prefixes/models and6bootstrapintervals independently verified. Notebook48cells executes; summarytable now uses confirmation, pilot remains distinct. Exact100-step globalmeans above3unchanged MTS examples and three matching-r late-time lattice heatmaps below them in one3x3 figure, each with32sensor markers and shared raw[0,1] scale; hashes/timing manifest andPNG/SVG inspected. Separate preview cell/artifact adds a full-width confirmation Q(r)/q(r) row; not yet adopted, accepted figure unchanged. Presentation backend import fixed (derive oldgrid from pilot, don't import Agg-setting CLI). Localroot data/order_parameter/cml2d_confirmation_260911 has rawbanks/compactreports/integrity-audit.json; fullMPIs/features gdata `/g/data/ql44/we2614/mts-spi-data/order_parameter/cml2d_confirmation_260911`. Physics4.9GBtar verified at `/g/data/ql44/we2614/mts-spi-archives/cml2d_confirmation_260911_physics.tar`, SHA51bb6c94934290426ce9e506557d8af2c606842a8159288cf803aa1da6fae707; originals preserved. Runtimee9cf5cc, auditbe3ca29, archival794a354 isolatedbranch; remote sourceworktree `/scratch/ql44/we2614/cml2d-confirm-source-5f76768` remains e9cf5cc. Main/unrelated edits unpushed. Finaljobs178774140–145,178776139,178776864; two launcher failures repaired without scientific changes/discarding cases, provenance inledger. Actualconfirmation1179.91SU inclfailures, pilot+confirmation1499.78SU; user removes cost ceiling. Eleven tests pass. Nearcritical halfdifference max.07912/blockSE.02313: finite-run global-Q interpretation retained. Claim: unsupervised order-related coordinate tracking across studied boundary from dispersed sparse observations, not formula discovery, numericQ calibration, longitudinal tracking or arbitrary-patch transfer.

COMPLETE CML2D: [protocol](../research/order-parameter-benchmarks/cml2d-protocol-260911.md), [results/ledger](../research/order-parameter-benchmarks/cml2d-results-260911.md). L256,N65536,r9,seeds26091111–18(firstfourdev),burn200k/obs2k/future1m. Physics42 local+12 convergence;72 primary masters→688 paired full-p90 views, plus2 smoke records. **Primary M32T1000: both gates pass, no exclusions/missing selected features; PC1 EVR.8027,19444 pairs/253 SPIs, leave-seed cos~.999. Held rho=.8983[.8516,.9546], controlmean.9833; q/Q steepest slope same3.86212–3.864.** Raw meanabsr stronger.9689. Within-r future-Q rho.0561; secondary same-window rho.5542[.0328,.7253], not longitudinal tracking. Same frozen model: T500/1000 rhos M8=.713/.768,M16=.826/.833,M32=.857/.898; no long-view exclusions and mean coordinate shifts<.092 SD. T100 rhos .663/.778/.793,12/216 exclusions within prespecified gate; shifts~-.18 to-.24 SD. **Contiguous M32T1000 fails:**10/40 excluded, no eligible held r3.84;14 retained held rho=-.169, q stays~1 despite falling globalQ. Raw patch baselines retain information; do not claim z-information absence or force a new fit. All688 input hashes and frozen-model identities verified; independent primary refit agrees8e-15; eight tests pass. Section7 notebook contains physics, CIs, M/T/paired agreement, failed layout and verdict. Data `data/order_parameter/cml2d_period_doubling_260911/`; costly cluster data tar on gdata, SHA766918b0…9421ab7 (full path/hash in report). All jobs including timeout retry34 and archive178711159 complete; total319.87SU<500. No M64, new physics after primary, or recurring reminder. Isolated branch `codex/cml2d-period-doubling`: primary62b73e7, sensitivity scoring9ed8aa4, archive95d1bcd; remote worktree `/scratch/ql44/we2614/cml2d-source-d619b02` remains9ed8aa4. Unrelated main commits remain unpushed.

CML2D scientific contract: Ising additions remain excluded. Q is the paper's symmetry-based observable, not an analytic solution Q(r,g); r_c=3.86212(12) at g=.2 is a numerical thermodynamic extrapolation, not an exact boundary. Alternate published cuts: r_c=3.8310(1) at g=.11 and g_c=.1100(1) at r=3.831. Q uses the absolute difference AFTER spatial averaging. Finite-N Q has a positive fluctuation floor even at infinite T. L64/128/256/512 size anchors support choosingL256; largerN does not eliminate short-view error or critical relaxation. Long reference seed ranges do not overlap at adjacent controls, but critical maximum half difference~.052 prevents an equilibrium-precision claim. Dispersed sites and patch locations were fixed independently of outcomes; no best-patch selection. All M/T views retain every step, avoiding period-two aliasing. Preserve across-control goal; no required longitudinal extension.

Candidate position: [comparative evaluation](../research/order-parameter-benchmarks/candidate-evaluation-260910.md) keeps SL/DZ/MH plus Kuramoto sufficient; the completed CML2D is now a viable additional canonical, exploratory example, not the old1D quadratic sweep. Published source https://arxiv.org/html/nlin/0605004: microscopic chaos both sides, FSS L32–128, illustrative diagramL1024. No forced Vicsek or new Ising-family implementation. Other candidates remain reserves; no further experiment is implied by the completed benchmark.

Final collection audit: all16 narrow Vicsek archives are local and pass source/config hash, finite-array, metadata, shape and mean checks;11 local regression tests pass. `data/order_parameter/vicsek_observation_260910/narrow-analysis/` contains all-seed curves, block/histogram plots and1920 M/T/view diagnostics with zero constant-channel fraction. At eta.468 histograms show two visible peaks, but stationary coexistence and SPI validity remain unestablished. Comparison notebook section6 now presents the physics-only cohort, all four intermediate-control traces, distributions and M/T checks; its builder was updated, all19 code cells executed without errors, and three new figure outputs were visually checked. Vicsek stays excluded from the SPI recovery metrics table. No background transfer remains.

Latest: **narrow Vicsek job178628828.gadi-pbs completed**, exit0,56m11s wall,29.96 SU; total overnight cluster charge37.17 SU. Sixteen N32768 cases, eta=.460,.468,.476,.484, seeds910204/910205, ordered/random starts; burn20k+record100k/stride25. All four seed/start mean-order curves decrease; control means .3442,.2269,.1170,.0773. Intermediate controls show pronounced time dependence and start/seed variation, so use measured window-level order rather than assume a precise stationary scalar per noise. No thermodynamic discontinuity or new SPI recovery established. [Final results](../research/order-parameter-benchmarks/vicsek-narrow-results-260910.md); [frozen protocol](../research/order-parameter-benchmarks/vicsek-narrow-protocol-260910.md). Isolated commit `eade8c2` on `codex/vicsek-narrow-260910` avoids pushing unrelated unpublished main commits. No more simulations or p90 launched. User requested stop: the overnight reminder `order-parameter-boundary-experiments-overnight` was **deleted**, confirmed by the app. Further work requires a new request.

2026-09-10 [candidate selection](../research/order-parameter-benchmarks/candidate-selection-260910.md) supersedes blanket Vicsek deferral; keep Kuramoto/Miller–Huse/Stuart–Landau. Earlier [Vicsek scout](../research/order-parameter-benchmarks/vicsek-pilot-results-260910.md):24 local cases plus completed long job178580950 (exit0,53m39s,7.15 SU), four N32768 cases, seed910203, pinned90543f1. Verified archives in `data/order_parameter/vicsek_observation_260910/gadi-long-90543f1/`, analysis in `long-analysis/`. Mean phi ordered/random .430053/.429941 at eta=.44, .105521/.092820 at .476. Initial smoke178580558 failed on Numba cache import (.04 SU); fixed smoke178580749 passed9 tests/2 archives (.02 SU). [Ledger](../research/order-parameter-benchmarks/overnight-260910.md). Small-view channels vary, not a full SPI-validity pass. Fixed particles differ from aggregate spatial bins; initially local particles disperse. Other new candidates remain reserves.

2026-09-10 [SL temporal audit](../research/order-parameter-benchmarks/stuart-landau-temporal-results-260910.md) complete:28 baseline archives (one reused)+five dt/burn sensitivities, N32/800, gamma .70,.74,.75,.8,.9,1,1.2. At gamma.74 SD(R)≈.1245 for N32 but≈1e-9 for N800; at .75 both fluctuate strongly. Half-dt changes in tested mean/SD <=1.1e-7; burn200→1000 changes meanR by -.00223 (N32/.8), -.00106 (N800/.75), but -.0231 at N800/1.2/seed910002: that endpoint remains unsettled. Store complex Z and temporal variability, not only meanR; no chaos claim from spectra. Uniform-grid frequencies mean seeds vary starts/permutations/sensors, not independent random frequency populations. Streaming code avoids O(NT), source/report87f6bb9; eleven combined local tests pass. Preserve previous confirmation labels, Ising exclusion and other workstreams. PRR5,013078(2023) is Zagli et al.; PRE110,014121(2024) is Evangelou et al.

Test whether an unlabeled SPI--SPI coordinate changes with an independently defined physical order parameter or dynamical-regime diagnostic as a control is swept.

- Run the complete `configs/pyspi/benchmarked_p90.yaml`; do not intentionally exclude an SPI family. Target-blind development-only validity handling, imputation, variance filtering, centring and reduction are allowed.
- For MPI edge vectors `v_a`, `z_ab=corr(v_a,v_b)` is a correlation-Gram geometry of estimator response profiles over channel pairs. It loses each MPI's affine level/scale and dyad labels, but an informative MPI can affect every pair containing it; the representation is not blind to individual-SPI information.
- Call `q` unsupervised only if every representation and hyperparameter choice is target/control blind. A later `q -> Q` calibration is supervised.

2026-09-16 SL clarification (no notebook edits requested): recommend fine gamma .680:.005:.770 for the local locking-to-unsteady claim; broad .55–1.25 is M/T robustness and wider-regime context, not a competing critical estimate. Stored N32 fine scores have meanR/SD(R)=.645381/~1.5e-16 at.680, .505110/~1.46e-10 at.720, .402235/.100986 at.725. Constant in time at fixed gamma does not imply constant Q across gamma. The shared steepest interval .720–.725 is sampled finite-run localization, not an exact gamma_c; .7225 is only its midpoint. Snapshot gamma.55 R is constant to~3e-16; gamma.725 first100 samples span only10 physical time units and hide most of its oscillation (full1000 SD=.10163). **Wording error to fix only when authorized:** lean notebook calls the frequency grid midpoint; generator line681 and stored N32/gamma.70 sensor frequencies verify endpoint-inclusive linspace[-gamma,gamma] (1.3–2.7 with carrier2). Results are unchanged; do not change the implemented population to match the mistaken prose.

## Kuramoto: full-catalogue retrospective result

- Reconstruction used all 289 p90 SPIs (`41,616` pairs), one Gaussian random-frequency population and the existing `M=20,T=1000,N=256` banks. Development-only 99% validity and SD `>=.05` gates retained 26,367 pairs spanning 244 SPIs; no family was deliberately removed.
- Frozen target-free PC1 explained `.359` of development variance. On 512 paired terminal rows, Spearman with future full-system `R_N` was `-.956` (`95% CI [-.966,-.945]`) overall and `-.628` (`[-.749,-.452]`) within coupling cells. Across-master PC1 variance peaked at reduced coupling `.9925`, near the known boundary. A separate supervised isotonic readout had MAE `.0589` (`[.0510,.0672]`). Mean absolute input correlation was stronger (`.975/.680` overall/within-control).
- Two terminal rows had 25.5% selected-feature missingness; excluding rows above 5% left `|rho|=.955/.629`. This is retrospective because outcomes were disclosed previously, although neither outcomes nor controls fitted the representation.
- The notebook presents this first and keeps the prospective 164-SPI non-phase Gaussian/logistic assay below a divider as historical provenance (`|rho|=.924/.933` overall).
- Natural frequencies `omega_i` are quenched equation parameters; no sampling law is uniquely canonical. Use Gaussian random frequencies as primary. A uniform law changes the transition, and extra logistic/quantile paths distract from the main question.
- Partial observation tested recovery of a hidden global future quantity from a sensor view and avoided self-inclusion. It is meaningful but secondary. A future primary generator should use feasible full observation (`M=N`, likely 20 or 32).
- Evidence: `notebooks/inference/kuramoto-order-parameter-confirmation.ipynb` and `data/order_parameter/kuramoto_full_catalogue_reanalysis/`; historical compact data remain under `kuramoto_final_confirmation_contract/`.

## Miller--Huse: best immediate next system

- Deterministic 2-D chaotic CML: `x'=(1-4g)f_mu(x)+g sum_nn f_mu(x_nn)`, using the original odd piecewise map at `mu=3`.
- It has a coupling-driven macroscopic symmetry-breaking transition. The original exponents were consistent with 2-D Ising behavior (https://doi.org/10.1103/PhysRevE.48.2528), but refined synchronous-update estimates, especially `nu=.887(18)`, suggest a distinct class while not excluding a very slow Ising crossover (https://doi.org/10.1103/PhysRevE.55.2606). Do not call the universality established.
- Use `Q_MH=<|L^-2 sum_r sign(x_r)|>_t`; RMS magnetization, susceptibility and Binder cumulant are sensitivities. The attempted `mu=1.9` path failed start-state convergence and remains excluded.
- The repository exactly matches the original synchronous `mu=3` map. `L={32,64,128}` scouts reproduce the sharp rise near refined `g_c=.20534(2)`, susceptibility peak and start-state agreement. A focused `L=128`, two-million-step future-truth audit passed with explicit uncertainty: p05 effective samples is `63.2`; adjacent 250k-block difference p95 is `.099`; at the hardest `g=.205` cell, median block-mean SE is about `.021` on a total Q range near `.8`. Use the two-million-step scalar and eight block summaries in p90; do not persist redundant future trajectories.
- Here physical `N=L^2`. Full p90 observation at adequate `L=64/128` is infeasible because every MPI scales as `N^2`; dataset parallelism does not remove per-row memory. Primary observation is a frozen nested dispersed sensor set `M={8,16,32}` from one full `L=128` trajectory; local patches and small full lattices are sensitivities.
- Full-p90 development completed all 288 rows with all 289 SPIs. Target-blind hygiene retained 32,836/41,616 pairs; PC1 explained `.2443` and had loading cosine `.976-.994` across M-specific fits. Held instances 4--7 gave row-level `rho(q,Q)=.9125`, pooled control-mean `.9650`, and per-M cell-mean `.9650-.9860`; within-control rho was only `.2381`. Long-truth block-mean SE was median `.00072`, p95 `.0193`. Temporal spectral entropy was stronger overall (`rho=.9607`) and control-only MAE `.00325` was far below q-readout `.0892`; retain as canonical unsupervised order-coordinate recovery, not predictive superiority. This remains development-only until independent controls/seeds are run.
- The sealed 216-row interleaved-control confirmation stopped before outcome access because one `M=8,g=.225` row had `.0596` selected-feature missingness (p99 over the bank was zero). A separately sealed target-blind sensitivity excluded that row at the unchanged 5% threshold: `rho=.8885` (95% CI `.8476-.9204`), pooled control-mean `.9500`, per-M cell-mean `.9500-1.0`, and within-control `.0148`. Temporal spectral entropy remained stronger (`rho=.9406`); q-readout MAE `.0744` remained far above control-only `.0136`. Treat as strong robustness evidence after a failed strict gate, not pristine independent confirmation.

## Portfolio, rather than a fixed three-system list

1. **Headline systems:** Miller--Huse passed full-p90 development as the canonical nonequilibrium transition; Stuart--Landau passed arm-specific independent confirmation after the joint partial-observation gate failure. The latter's `K=.8`, `gamma=.6,.8,1.0,1.2` path is exactly Fig. 1 of Matthews--Strogatz, not an arbitrary slice.
2. **Secondary/negative stress test:** the existing one-dimensional quadratic CML at `eps=.3`, varying `alpha`. Its regimes are literature-grounded, but no accepted scalar order parameter spans the path and simple baselines substantially outperform q. Keep that experiment as a limitation or appendix result; do not run its confirmation. A distinct two-dimensional quadratic/logistic CML has a much stronger local target: the collective period-1--period-2 transition below.
3. **Best next canonical backup:** periodically driven kinetic Ising, with stochastic spin dynamics and cycle-averaged magnetization as the established dynamic order parameter (https://doi.org/10.1103/PhysRevLett.81.834). This is scientifically distinct from the deleted equilibrium/static Ising assay, but do not implement it without resolving the user's prior instruction to remove Ising-family work.
4. **Deferred:** Vicsek has canonical polarization but requires large physical populations for defensible transition behavior; the contact process has canonical active-site density but an all-zero absorbing phase that degenerates p90 inputs; CGLE and neural-chaos models are useful application bridges but lack an equally clean single observed `Q` or require a modified driven/noisy process.

Application bridges are a separate tier. Sompolinsky--Crisanti--Sommers gives a clean neural fixed-to-chaos transition; Brunel gives noisy irregular E/I spiking; Epileptor/Jansen--Rit gives seizure-like bifurcations; power-grid swing equations give noisy synchronization loss. None is a faithful generic proxy for real data. There is no accepted scalar consciousness order parameter, and no universal clinical seizure order parameter across onset types.

## Next sharp-boundary search

- A fine physics-only Stuart--Landau scout at fixed `K=.8`, `gamma=.60:.01:.82`, `N={32,64,128,800}` and three matched starts isolates the published locking-to-unsteady boundary. The maximum `|d<R>/dgamma|` interval and first `sd(R)>=.02` occur at `.72--.73` for `N=32`, `.73--.74` for `N=64`, and `.74--.75` for both `N=128` and `N=800`. This is a reproducible finite-size shift toward a stable large-N boundary, not an arbitrary visual cliff. Evidence: `configs/scout/stuart-landau-locking-boundary.yaml` and `data/order_parameter/stuart_landau_locking_boundary/`.
- Fine full-p90 confirmation uses 19 controls `gamma=.680:.005:.770`, eight new matched seeds, `M=N=32,T=1000`, and the existing frozen coordinate without refitting. Production/reconstruction/analysis jobs `178138842`, `178138850` and `178142831` finished successfully. The pre-outcome eligibility record passes all six gates: 152/152 rows retained, eight per control, maximum selected-feature missingness `.0355`, one clean production commit/config/p90 hash, and frozen schema/SPI order. Held-out `rho(q,<R>)=.8860` (clustered 95% CI `[.8819,.8933]`), control-mean `rho=.8965`, and `rho(q,sd(R))=-.9326`. Most importantly, the steepest interval is exactly `.720--.725` for `q`, `<R>` and `sd(R)`, agreeing with the independent `N=32` scout while retaining the known large-N shift to `.74--.75`. Within-control `rho=-.2196` (CI `[-.5644,-.0328]`), so this supports macroscopic transition tracking/localization, not realization-fluctuation recovery. Analytic phase coherence is slightly stronger overall (`rho=.9152`). Frozen supervised q-readout MAE is `.0314`, below the coarse-control-only readout `.0862`, but prediction is secondary. Two older smoke rows were legitimately regenerated for homogeneous provenance; their time series/targets are bitwise identical and only four stochastic soft-DTW barycenter SPIs differ, with unchanged validity patterns. A metadata parser bug found before outcome access was fixed and tested in `9d89aa1`. Evidence: `configs/analysis/stuart-landau-locking-boundary-confirmation.yaml`, `data/order_parameter/stuart_landau_locking_boundary_confirmation_analysis/`, and the executed comparison notebook.
- **Desai--Zwanzig interacting diffusions passed the second fine benchmark.** The canonical SDE and parameters are Eq. (1) of Evangelou et al., PRE 110, 014121 (2024): `alpha=1, theta=4, sigma_m=.8, nu=.5`, varying additive-noise amplitude `sigma`; the canonical first moment `M1=mean_i(x_i)` undergoes a mean-field pitchfork at `sigma_c=1.890`. A 2024 paper already recovered `M1` with diffusion maps, making this a verified benchmark rather than a novelty claim for latent phase-transition recovery. `N=12000` reproduces the population used in that paper and its cited 2023 phase-diagram study; it is precedent, not a derived convergence threshold. A new Milstein finite-size gate completed `N={32,100,1000,10000,100000}` over the transition. The steepest finite-run-Q intervals move from `1.40--1.50`, through `1.70--1.75` and `1.75--1.80`, to `1.85--1.875` for both largest sizes; maximum slope rises `.843,1.362,2.674,4.430,5.486`, and the connected-fluctuation peak moves `1.60,1.70,1.825,1.85,1.875` toward `1.890`. This is finite-time evidence, not equilibrium finite-size scaling: maximum start-state Q range rises from `.0053` at `N=10000` to `.0508` at `N=100000`, indicating critical slowing/insufficient equilibration. `N=10^6` was computationally attempted but deferred until the time horizon is scaled and convergence is audited; larger N alone would overstate validity. Evidence: `configs/scout/desai-zwanzig-finite-size-scaling.yaml` and `data/order_parameter/desai_zwanzig_finite_size_scaling/`. Full-p90 local confirmation used `sigma=1.75:.01:1.95`, eight instances, primary `N=12000,M=32,T=1000` partial observation and `M=N=32` sensitivity. All 336 rows and 289 SPIs passed the sealed target-blind gate; 9,210/41,616 pairs spanning 269 SPIs were retained, PC1 EVR was `.536`, and leave-instance loading cosine was `.993-.997`. On held instances 4--7, primary `rho(q,Q)=.9472` (95% CI `[.9010,.9877]`) and control-mean `rho=.9948`; both curves' steepest interval is exactly `1.86--1.87`, `.025` below the mean-field boundary. The normalized largest step is `.1831` for q versus `.1184` for Q; paired-bootstrap q-minus-Q CI `[.0413,.0953]`, supporting enhanced transition contrast but not a more physical order parameter or super-resolution critical estimate. Within-control `rho=-.2995` (CI `[-.9089,.5668]`), so the claim is macroscopic tracking/localization, not realization-fluctuation recovery. Pooled input SD is stronger (`rho=-.9795`), and control-only isotonic MAE `.0197` beats q-readout `.0251`; common representation, not optimality, remains the point. The `M=N=32` arm tracks its own rounded finite-run Q but the studied thermodynamic interval is already far beyond its shifted crossover.
- The exact-prefix `T={100,500}` sensitivity applied the unchanged `T=1000` transform. Its joint gate failed before outcomes: 166/168 `T=100` rows exceeded 5% selected-feature missingness (median `.0523`, max `.0893`), leaving no covered confirmation cell, so no `T=100` q--Q result is reported. A separately sealed target-blind `T=500` arm retained all 168 rows with zero selected-feature missingness: held `rho(q,Q)=.9341` (95% CI `[.8837,.9972]`), control-mean `rho=1.0`, paired `rho(q_500,q_1000)=.9774`, and q's steepest interval `1.85--1.86`, one grid step below `T=1000`. This is an arm-specific sensitivity after a failed joint gate, not a pristine joint confirmation.
- Best new non-Ising candidate: ZGB catalytic surface reaction with nonzero CO desorption. It has a published `(CO feed y, desorption k)` phase diagram and discontinuous active/high-CO line. `Q=theta_CO`; patch coverages/reaction counts remain dynamic because `k>0` removes the absorbing CO-poisoned state. Endpoint estimates are method-dependent: older coexistence finite-size scaling gives `k_c~.0406` (https://doi.org/10.1103/PhysRevE.71.031603), whereas a 2018 time-dependent pseudocritical analysis reports `(y_c,k_c)=(.554,.064)` (https://doi.org/10.1103/PhysRevE.98.032113). The planned `k=.02` cut is safely below both. Exact random-sequential pilots reproduce the expected sharp hysteresis. At `L=64`, empty/high-CO starts jump over `.534--.535`/`.528--.529`; at `L=100` with 5k-MCSS burn and record they jump over `.534--.535`/`.530--.531`, bracketing the published coexistence region. The maximum start-state gap remains `.87`, and the `L=100` coverage block-range p95 is `.85` because metastable switching contaminates finite records. This is physically promising but not p90-ready: bimodality, finite-L scaling and an explicit branch/coexistence estimand are required, so it is less simple than the fine Stuart--Landau intercept. Evidence: `scripts/zgb_desorption_physics_scout.py` and `data/order_parameter/zgb_desorption_physics_scout*/`.
- Best untested full-observation candidate: the Hamiltonian mean-field (HMF) rotator model, a widely studied long-range Hamiltonian system. Its canonical magnetization `M=|N^-1 sum exp(i theta_j)|` vanishes continuously at the exact thermodynamic `U_c=3/4` (`T_c=1/2`); the original APS paper is https://doi.org/10.1103/PhysRevE.52.2361, and published finite-N comparisons use `N=100,1000`. `M=N=32--100` is defensible as a rounded precursor, unlike a too-small spatial lattice. The main caveat is severe quasistationary/initial-condition dependence, so an equilibrium protocol and relaxation audit are mandatory. Empirical p90 scaling predicts `M=50,T=1000` near 57 minutes/row versus 23 minutes at `M=32`; use a small nested pilot, not a full bank. Coupled Rössler networks offer an exact finite-dimensional synchronization boundary via master-stability analysis (https://doi.org/10.1103/PhysRevLett.80.2109), but are less attractive because the order quantity is operational and the question overlaps Kuramoto/Stuart--Landau.
- Strongest formal dynamic-Q literature remains periodically driven kinetic Ising: cycle-averaged magnetization and explicit dynamic phase diagrams (https://doi.org/10.1103/PhysRevB.52.6550). It is genuinely stochastic dynamics, unlike the deleted static Ising assay, but implementation remains conditional on resolving the user's broader Ising exclusion.
- **Exact Kaneko-CML revisit:** scope means the existing one-dimensional diffusive ring `x_i'=(1-eps)f_alpha(x_i)+eps/2[f_alpha(x_{i-1})+f_alpha(x_{i+1})]`, not the two-dimensional Marcq CML. The strongest verified order-parameter target is Gade--Sahasrabudhe's persistence phase diagram (https://doi.org/10.1103/PhysRevE.87.052905): its lower critical curve separates zero persistence/moving defects from a modulo-two frozen, long-range antiferromagnetic state. Their logistic `mu` convention is conjugate to ours by `alpha=mu(mu-2)/4`, preserving `eps`; published critical points therefore include `(alpha,eps)=(1.5725,.015),(1.640625,.045),(1.71,.065),(1.8816,.104)`. Modulo-1 or modulo-2 persistence is the published dynamical order parameter; defect density or staggered spatial order is a complementary instantaneous observable. This is asymptotic coarsening: the paper used `N=8000`, 400k transients for the plane and `N=50000` for critical scaling, so a physics-only size/time gate is mandatory. At `alpha=2`, later exact-ring work (https://doi.org/10.1016/j.chaos.2023.113510) finds zigzag ordering for `eps=.124--.164` and defines phase-defect density, while Lacasa et al. (https://doi.org/10.1038/srep15508) give a finite-`N=200` regime intercept: FDT `<.12`, defects/STI `.12--.15`, periodic pattern selection `.16--.20`, FDT `>.20`, with visibility-network `I`/edge overlap as operational order parameters. Those network quantities are too close to SPI--SPI to serve as an independent headline truth. Pattern-selection-to-FDT otherwise has accepted diagnostics but no verified unique canonical physical scalar. The old `eps=.3, alpha=1.6--2.0` path must not be retrofitted with one. Any new claim-bearing run needs a large physical ring and a size/time ladder; low `M=N` is a finite-size/commensurability diagnostic only.
- Inertial noisy Kuramoto has a canonical coherence `R` and published first-order/hysteretic surface (https://doi.org/10.1103/PhysRevE.89.022123), but is scientifically too close to the existing synchronization baseline. Vicsek polarization is canonical but faithful phase-boundary work needs very large populations and long band-coexistence runs, forcing partial/coarse observation. Keep both as backups.
- Do not lead with homogeneous Brusselator Hopf or Swift--Hohenberg onset: their canonical bifurcations are clean, but post-transient spatial channels become identical or stationary, making the MTS/SPI problem degenerate unless noise or heterogeneity changes the benchmark. Do not lead with CGLE phase/defect turbulence because the relevant boundary is finite-time/nucleation sensitive.

## Analysis contract

1. Reproduce physical behavior without pyspi; establish burn-in, stationarity, finite-size/time convergence and independent seeds.
2. Freeze controls, physical `Q`, observation, development and untouched confirmation masters; then run full p90 on Gadi.
3. Fit feature hygiene and reduction on development rows only. Use PC1 only if target-blind one-dimensionality and bootstrap/leave-group stability pass. Curved geometry can use a frozen out-of-sample PCA(d)-to-manifold map chosen by eigengap/connectivity/stability, never `Q` correlation. Branching/hysteretic planes need at least 2-D or branch-specific coordinates.
4. Evaluate held-control/seed `q--Q` association, within-control association, and a separate supervised decoder only if numerical inference is claimed. Compare raw/simple, control-only and individual-SPI baselines.

For `M,T` transfer, keep physical `N` fixed, take nested channel views and prefixes from many independent maximal trajectories, and apply one common development-fitted mask, scale and embedding. Start with `M={8,16,32}`. Separately fitted/standardized coordinates are not comparable; nested views are paired sensitivities, not independent replicates.

Current sample-length contract is `T={100,500,1000}` for the selected robustness system: fit the primary transform on `T>=500` and apply it to `T=100` as a stress cell. Use paired `T=2000` only to test whether it materially improves feature/coordinate stability. Existing p90 validity gains from 1000 to 2000 are small; precision remains to be measured by paired prefixes.

Stuart--Landau is the selected full `M x T` robustness system. Initial Miller--Huse and quadratic-CML p90 development uses `T=1000` across `M={8,16,32}`; add shorter prefixes only after a useful coordinate is demonstrated.

Stuart--Landau full-p90 development passed its target-blind geometry gate: all 289 SPIs entered; ordinary hygiene retained 24,466 pairs spanning 234 SPIs; PC1 explained `.661`, was the strongest of the first ten PCs on the held diagnostic, and had loading cosine `.977-.987` across all six `M x T>=500` source fits. On instances 4--7, per-cell mean `rho(q,Q)=.85-.99` and the pooled gamma-mean curve has `.976`; raw pooled rows have `.756`. `T=100` is useful as a cell-level stress test but its paired q-rank agreement with `T=1000` is only `.628/.740/.944` at full `M=8/16/32`; `T=500` improves to `.777/.909/.957`. Do not add `T=2000`. The sealed 1,296-row joint confirmation failed before outcome access because 14 secondary partial-observation rows exceeded the predeclared 5% selected-feature-missingness threshold (max `.0785`, p99 `.0526`). All 648 primary full-observation rows passed (max `.0266`), and their separately sealed arm-specific confirmation gave row-level `rho=.7485` (95% CI `.7356-.7607`), within-gamma `.3306`, and pooled gamma-mean `rho=1.0`; cell-mean rho was `.833-1.0`. Analytic phase coherence remained stronger (`rho=.9299`, within-gamma `.7521`), and frozen-q isotonic MAE `.0407` was worse than control-only `.0334` (paired bootstrap difference CI `.0056-.0091`). The claim is feasibility of changing order-coordinate recovery, not superiority, microscopic fluctuation prediction, or information beyond the known control.

Both confirmation banks show that an all-bank maximum-missingness gate is too brittle: a few near-constant ordered/sensor views can invalidate hundreds of otherwise clean rows. Do not rewrite these gates retrospectively. Future preregistration should instead specify target-blind per-row exclusion, a maximum excluded fraction, and minimum retained coverage per control/M/T cell.

Claim-bearing p90 sweeps are one-dimensional by default. A cheap two-control physics map may validate an intercept (already done for Stuart--Landau), but do not expand it to the full `control x M x T x instance` design unless no one-dimensional path separates the relevant regimes.

The quadratic-CML two-million-burn gate covers `10 alpha x 3 N x 8 seeds`, plus a dense `N=512,alpha=1.71:0.01:1.80` check. Four consecutive 5k truth blocks are stable away from the reorganization; over the merged gate, pooled physical-diagnostic prefix-error p95 is `.194/.119/.096` at `T=100/500/1000`. All eight `N=512` seeds are low-temporal-entropy patterned through `alpha=.73`; coexistence occurs over `.74-.76`; all eight are high-entropy by `.77`. At `.75`, five seeds are low- and three high-entropy. Do not average the branches into a scalar truth. The p90 gate passed: use fixed `N=512,M={8,16,32}` on `alpha=1.60:0.01:2.00`, with ten anchors for separate `M=N={8,16,32}` finite-size arms. The physical vector is temporal spectral entropy, dynamical spatial-pattern entropy, selected-band power and period-two residual.

Quadratic-CML full-p90 development completed 1,224 rows with all 289 SPIs; hygiene retained 15,673/41,616 pairs and q-PC1 explained `.4107`. The four-observable physical vector is `.9519` one-dimensional on this intercept. Held large-lattice q1 versus physical-PC1 has `rho=-.7332`, while 2-D distance geometry is `.6669` overall and `-.2325` within alpha. Mean absolute correlation and temporal spectral entropy are stronger (`|rho|=.9306/.9501`); supervised q-vector MAE `.1518` is only slightly below the two-input baseline `.1603` and far above control-only `.0630`. Do not promote or confirm this system.

The executed paper-facing synthesis is `notebooks/inference/order-parameter-benchmark-comparison.ipynb`, reproducibly built by `scripts/build_order_parameter_benchmark_comparison_notebook.py`. It separates recovery/tracking, within-control recovery, supervised prediction and transition localization; states the observation contract for every system; and plots physical `Q` against every available `q_M` with bootstrap 95% confidence intervals. Stuart--Landau additionally overlays `T={100,500,1000}` within each `M=N` panel. Its phase plane is explicitly a coarse repository reproduction; published analytical Fig. 2(a) remains authoritative. Stripped robust-per-process MTS examples at the low, transition/reorganization and high controls live under `notebooks/inference/figures/{kuramoto,stuart-landau,miller-huse,quadratic-cml}/` and are reproducible with `scripts/export_order_parameter_mts_heatmaps.py`.

## Minimum figures

Plot control versus physical Q and sign-oriented q with independent-seed uncertainty; q-versus-Q scatter coloured by control is optional, and any numerical calibration is supervised. Two-control claims require Q/boundary and frozen-q maps, not one arbitrary path. Orientation uses exact equation/control/Q and robust per-process icefire MTS/field snapshots as context, not evidence. Preserve the explicit one-control cuts: MH varies g; SL varies gamma at K=.8; quadratic CML varies alpha at eps=.3, crossing finite-time reorganizations rather than precise asymptotic phase lines.
