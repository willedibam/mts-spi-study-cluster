# Small-lattice full-observation CML diagnostic

## Final outcome, 15 September 2026

Both requested small-full-system arms are resolved without a successful order-inference claim. L=6 production 178982490 and report 178982491 exited 0 (40m26 and 2m01). All 680 rows are eligible for the separate L=6 fit, but its 40 retained meta-features fail the prespecified geometry-stability gate: PC1 EVR=.976535, minimum leave-seed loading cosine=.223817 (<.8). Development seed 260914007 supplies both non-period-four development observations (r=3.850,3.854); these rare attractor-class differences drive the apparent dominant direction. A high EVR alone is therefore misleading. The figure may show its q only with an explicit failed-gate/descriptive label, as requested by the user.

Unchanged L=256 transfer fails for all 680 rows: selected-feature missingness ranges from 45.60% to 99.95% (median 54.49%), with zero eligible evaluation scores. L=8 had already stopped on 50 raw constant-channel recordings. Structural checks support genuine stable periodic attractors and basin/geometry effects, not an uncorrected execution failure; no clean M=N<=64 workaround was established. This does not alter the independently confirmed large-L result.

Independent verification covered the manifest/archive hash, 680 row identities and targets, 17×(8+32) role coverage, common recorded execution identity, recomputed missingness and gates, primary q reconstruction (maximum error 3.55e-15), an independent SVD/leave-seed stability calculation, and exact equality of the transferred model arrays. No fresh raw-MPI replay was done. Compact outputs are local under `data/order_parameter/cml2d_full_observation_260914/L6/{analysis,transfer-analysis}`. The notebook executes successfully and shows the L=6 descriptive failed-gate q/Q curve; its composite was visually inspected. Accepted large-L PNG and manifest hashes remain unchanged. The app confirmed deletion of `monitor-cml-full-observation-diagnostics` on 15 September 2026. This work is complete; earlier execution entries below are historical.

## Question and fixed design

User-requested diagnostic: does an unsupervised SPI–SPI coordinate track the finite-system period-alternation observable when every lattice site is observed? This changes the physical system size, not merely the observation layout. A failure at L=6 or L=8 does not invalidate the existing L=256 dispersed-observation result. The thermodynamic transition at r=3.86212(12) is only a reference marker for these small lattices, not their assumed transition location.

Use the unchanged synchronous periodic-square logistic update, g=0.2, all 17 controls from the completed large-system confirmation, 200,000 burn steps, 2,000 recorded observation steps followed by a disjoint 1,000,000-step Q reference. Two arms: L=6, M=N=36 and L=8, M=N=64; primary T=1000. All sites are recorded once in fixed row-major order, without noise injection, temporal decimation, sensor selection or preprocessing changes. All 289 p90 SPIs are attempted. No extra M,T grid completion, longer-reference or finer-control experiment is launched under this diagnostic.

Eight development seeds 260914001–008 and 32 evaluation seeds 260914101–132 at every control and size give 680 recordings per arm, 1,360 total. Controls are paired within seed; uncertainty resamples seed clusters, not sites or controls. Seeds do not select the best-looking attractor. Fixed figure examples use the first evaluation seed at r=3.84, 3.86212 and 3.89 and the first 100 input steps. Lattice images are the saved final fields, explicitly later than the illustrated input.

## Separate inference and transfer questions

Primary: independently fit the existing target-blind hygiene and PC1 procedure within each L using development seeds only, then freeze it for evaluation. Keep the same minimum feature validity 0.99, variance threshold 0.05, row missingness limit 0.05 and one-coordinate geometry gates (EVR >=0.2, PC1/PC2 >=1.5, leave-seed loading cosine >=0.8). A display-only sign may use development Q as in the existing analysis; no target/control selects the component or representation. Report failed geometry or row gates explicitly, without searching alternative components. This is a prespecified held-out diagnostic, not a retroactive claim of thermodynamic criticality.

Secondary: apply the original L=256 frozen model unchanged to the same recordings. A transfer failure is not a failure of the arm-specific inference test. Never renormalise the transferred q within an arm. Independent fits have distinct coordinate units, and are not claims of the same coordinate across physical sizes.

For each analysis, at most 10% of evaluation rows may exceed selected-feature missingness, with at least 24 of 32 evaluation rows retained per control. For the new fit also report development coverage, requiring at least six of eight per control. Seal geometry and eligibility before endpoint reporting. Report the planned/retained denominators even if raw constancy or extraction fails; no fabricated or silently dropped cases. Do not plot a successful q tracking result if its gates fail; show the failure and the physical curve instead.

Endpoints: held-out q–Q_reference Spearman with 2,000 seed-bootstrap resamples, control-mean association, sampled steepest-slope interval (descriptive, not a critical-point estimator), matched-window Q and within-control association as secondary diagnostics. Retain mean-absolute-correlation and same-window sampled Q baselines. For full observation, sampled Q is exactly Q_window, so it is a transparent direct baseline, not a competing hidden-state estimator. Inspect Q blocks, half-reference differences, input channel variance and redundancy. A finite-N absolute-difference floor or small-system attractor change must not be labelled thermodynamic ordering.

## Predictions recorded before simulation

Expect more rounding, possible displacement from the large-lattice transition and stronger initial-condition/attractor effects. The ordering between L=6 and L=8 is not assumed monotonic. Full observation removes spatial subsampling error but does not restore large-system physics. It may improve recovery of each small system's own Q, while increased channel redundancy or periodic behaviour may reduce SPI feature validity or make the large-system direction uninformative. No numerical correlation or clean transition is predicted.

## Execution and evidence

Explicit monitoring authorised: the app heartbeat `monitor-cml-full-observation-diagnostics` checks this task every 20 minutes, stays quiet for unchanged/non-actionable state, and continues only the agreed work. Delete it after the verified notebook/report and diagnostic are complete; pause it and ask once if a material scientific choice is needed. The older deleted reminder has not been restored, and the separate HCP monitor is untouched.

Production recovery: smoke 178978036 and node batch 178978037 both passed, giving 24 reusable p90 outputs. Controller 178978099 failed before launching anything because the bare `python` command resolves to Gadi's deprecated alias. Fix 338a29f uses `python3`; the immutable p90 runtime remains 06b3f76. The controller was rerun as a lightweight login-node submission command, with its one-shot lock verified absent beforehand. New production job 178982490 and dependent report 178982491 are submitted. Timing median/max are 1275.692/1831.783 seconds. The farm uses 680 dataset workers, 2,064 allocated cores, 8,170 GB and a one-hour wall limit, with a 3,048-second task timeout and validated skip-existing behaviour. Memory allowance is conservatively 12 GB/worker because the one-record smoke peaked at approximately 9.65 GiB; the representative node peaked at approximately 47.75 GiB aggregate. No records, scientific thresholds or fitted-model rules changed. The current submission ledger is `production-submission-L6.json` on gdata.

Structural scout and aggregate completed successfully (178978103: 78 seconds; 178978104: 19 seconds). All 620 planned cases and 1,360 audit identities were verified locally, together with source hashes and the reductions of Q from its eight blocks/two halves and Lyapunov estimates from ten blocks (agreement within 1e-12). Nineteen local tests still pass. CSV summaries and completion record are now local under `data/order_parameter/cml2d_structure_260914/`; notebook tables and plots use those results.

Structural interpretation: all 120 random-start cases at L=24,32,64 have positive late-time largest Lyapunov estimates and decreasing mean Q across the five sampled controls. Endpoint mean Q changes are 0.3330 to 0.1513 (L=24), 0.3336 to 0.1161 (L=32), and 0.3338 to 0.0572 (L=64); finite-size rounding remains substantial. L=24 is the first tested size with this consistency, not a universal minimum. L=12 and L=16 show some negative late exponents and maximum reference half differences of 0.2438 and 0.2931, indicating attractor changes or nonstationarity in some runs. Maximum half differences at L=24,32,64 are 0.00567, 0.00790 and 0.01642.

No clean full-observation workaround at N<=64 is established: L=5 remains mostly periodic; L=7 alternates between different chaotic and periodic states with nonmonotone Q, rather than reproducing the intended intercept. L=6 remains periodic under the two-million-step burn and tested preparations. At L=8 the longer burn makes all eight r=3.89 cases approach Q approximately zero, with constant sites; low-r preparation sometimes sustains different chaotic outcomes at this endpoint, but does not give a consistent, nondegenerate replacement across the tested cases. Recurrence describes the early saved observation window and Lyapunov describes a later segment after the reference, so differing classifications need not conflict. Do not turn the first successful tested size into a sharp minimum-size claim or search new p90 arms without the agreed decision point.

Resumed after user approval: L=6 now has an independent smoke `178978036`, representative node `178978037`, and L6-only one-shot continuation `178978099`; the unchanged runtime/corpus is reused. The new controller accepts explicit lattice arms and uses per-arm submission/lock files, so L=8 no longer blocks L=6. The smoke was observed running; production/report jobs will be created only after the measured-timing stages pass and recorded in `production-submission-L6.json` on gdata. Other users' or workstreams' jobs are untouched.

The structural follow-up is pinned to `b22cc9da92ef172353a7970d4b76fd9de3d35cff` in `/scratch/ql44/we2614/cml2d-structure-source-b22cc9d`. Jobs: existing-bank audit `178978101` (96 cores), two-case structural smoke `178978102`, 620-case scout `178978103` (96 cores, dependent on smoke), and aggregate `178978104` (dependent on audit and scout). Output root: `/g/data/ql44/we2614/mts-spi-data/order_parameter/cml2d_structure_260914`. Nineteen local tests pass, including an independent finite-difference tangent check, dense-Jacobian agreement, a fixed-point Floquet/Lyapunov oracle, and exact case-count checks. The structural smoke covers L=6 and L=64 at r=3.84; validated results are reused in the full scout. No recurring automation was created.

Completed existing-bank audit: job 178978101 exited 0 in 53 seconds and produced all 1,360 records, copied locally under `cml2d_structure_260914/audit/`. L=6 has 672 period-four records, all with sub-unit Floquet radius, and eight without detected recurrence <=32. L=8 has 50 period-two, one period-three, 180 period-four, two period-six, 39 period-eight and 408 without detected recurrence <=32. Of its 272 detected cycles, 271 are linearly stable and one is weakly unstable. All 50 constant-channel failures are period-two fields. These classify the saved observation window, not the entire future reference, and nonrecurrence is not a chaos label. Structural smoke 178978102 also exited successfully (46 seconds); the full scout was queued at the last check. L=6 p90 smoke was still running, without a completed q result.

Targeted post-audit validation of the single weakly unstable orbit: L=8, r=3.84, seed 260914131, original case 718. Its radius is 1.00097154 and tangent exponent +0.000242767 per step. Restarting the saved final state without perturbation keeps the repeating orbit and Q=0.00550690377. Independent diagnostic restarts with amplitude 1e-12 or 1e-8 both reach the nearby stable period-four orbit (radius 0.99902637, exponent -0.000243526, Q=0.00551080804). Each diagnostic used the standard 200k burn and disjoint million-step reference. Thus one exceptional trajectory exhibits numerical locking onto an unstable cycle, but the perturbation does not restore microscopic chaos or the intended macroscopic boundary; it does not explain the hundreds of stable periodic states. These three local validation continuations are separate from the 620-case prespecified scout, and no benchmark data or targets were replaced. Reproduction: `diagnostic-masters/case-718.npz`; `default_rng(260914718).normal(size=(8,8))`, subtract its spatial mean, divide by its maximum absolute value, add the specified amplitude to `final_state`, then use `evolve`, `orbit_diagnostics` and `lyapunov_blocks` with the recorded parameters.

Initial local linear-stability calculations on the six predetermined saved examples: L=6 period-four orbit spectral radii are 0.732652, 0.725505 and 0.715079 at r=3.84, 3.86212 and 3.89, respectively. These are below one for the full 36-dimensional return-map derivative, supporting locally stable periodic attractors, not only apparent finite-precision recurrence. These examples are identical across lattice rows (stripe patterns). L=8 at r=3.89 has a period-two orbit radius 0.933226, also below one despite 16 constant sites and globally cancelling oscillations. The L=8 r=3.84 period-four orbit is weakly stable (0.999026); no period <=32 was detected for the boundary example. This does not establish basin sizes or a minimum L; the full audit, perturbations and size/preparation checks address those uncertainties.

An additional exact-orbit diagnostic rules out a simplistic size-only explanation for those stripes: tile each recorded L=6 period-four field onto L=12,18,24, then form the full 144-, 324- and 576-dimensional four-step Jacobian product using `coupling_matrix`. At all three anchors, the largest multiplier remains respectively 0.732652, 0.725505 and 0.715079, agreeing with L=6 within 1e-14. Those periodic solutions therefore remain linearly stable at the tested larger multiples of six; larger size does not automatically destroy them. This supports investigating basin prevalence and commensurability rather than asserting a universal minimum size. It does not establish stability at all L or quantify the probability of reaching those tiled states from random starts. Reproduction uses the last four full fields in each fixed-seed L6 `primary-masters` archive; tile with `np.tile(cycle,(1,L//6,L//6))`, and multiply `C * [r*(1-2*x_t)]` over the four phases before taking the largest eigenvalue modulus.

The notebook now has 55 executed cells without errors. Both small-L subsections display real physical composite figures, including full-lattice fields, with a physical-Q-only bottom row clearly labelled p90 pending (L=6) or raw gate stopped p90 (L=8). Numerically constant channels are masked grey solely for display; no raw data are changed. L=8 examples are read directly from the physical masters rather than inventing a passed p90 corpus. Figure provenance JSON records hashes and timing; all six master samples and the L=6 raw corpus were transferred locally. The accepted large-L figure PNG and manifest hashes are unchanged. Structural results are explicitly pending.

Current status after the first attempt: all 1,360 physical simulations completed (physics job 178965343, exit 0, 87 seconds). Export job 178965344 stopped with exit 1 on the prespecified raw-channel variability check for L=8, after successfully exporting all 680 L=6 recordings. No p90 smoke, node, production or report job ran: the failed shared export dependency prevented both arms and their continuation. No new job was submitted during the status check. The remaining work requires separating the valid L=6 path from the L=8 diagnostic, not rerunning the completed physics.

L=6 has zero raw-channel failures in 680 recordings. For its 32 evaluation seeds, mean Q at r=3.84, 3.86212 and 3.89 is respectively 0.02287395, 0.02979800 and 0.03971007. This endpoint trend is opposite to the large-L decreasing curve; no SPI–SPI result or canonical small-L transition is established. Maximum reference half difference across this arm is 0.00000227.

L=8 has 50/680 recordings with at least one channel SD <=1e-8; 38/544 are evaluation recordings. Evaluation failures are concentrated at r=3.875 (1/32), 3.8825 (14/32) and 3.89 (23/32), so simply dropping those cases would violate the specified minimum 24 retained evaluations per control. Direct checks of the first failing archive at each affected control found exactly 16 constant channels out of 64, while the other channels still vary (maximum channel ranges 0.576–0.582). These are not wholly static recordings: near-zero global Q can coexist with local oscillations and constant sites. The maximum reference half difference across L=8 is 0.16918, indicating substantial finite-run instability in at least one case. No jitter, channel removal, initial-condition selection or threshold relaxation has been applied.

Evidence: both arm-level `physics.csv` files and the export traceback on Gadi. Checked raw examples: (r=3.875, seed=260914122), (3.8825, 260914003), (3.89, 260914001). All raw physical masters remain available; notebook q figures are still pending, not completed results. The shared-export coupling that blocked otherwise valid L=6 is an execution-design limitation, distinct from the genuine L=8 input degeneracy.

Read-only saved-trajectory diagnosis, fixed illustration seed 260914101 (not selected for appearance): over the last 500 of the 2,000 saved observation steps, the entire L=6 field repeats after four steps at r=3.84, 3.86212 and 3.89 (maximum lag-four residual 0, 0 and 2.11e-15), but not after two (residuals 0.134, 0.169 and 0.205). This establishes period-four behaviour in these recorded examples, not a classification of all seeds or a mathematical stability proof. L=8 at r=3.84 also has a period-four microscopic field, while its late global mean has constant parity branches. At r=3.89 the L=8 field repeats after two steps exactly, with 16 constant sites, median local temporal SD 0.204, but Q=1.11e-16: local oscillations cancel in the global mean. The L=8 boundary example does not repeat at lags 2, 4 or 8 to numerical precision; no chaos diagnosis is inferred from that fact. Thus the small-lattice differences are not established to be mere rounding of the same macroscopically ordered/microscopically chaotic transition. Possible q–Q tracking at these sizes remains untested; finding a sharp finite-system boundary would be a separate physics result, not automatic recovery of the published large-lattice boundary. No jobs or model changes were made for this diagnosis.

Use an isolated pinned source branch, shared-source compute workers, immutable inputs, and result corpora on gdata because Scratch inode headroom is limited. Run a two-record p90 smoke (one per size), then a representative node batch before selecting homogeneous many-core production farms from measured runtime and memory. Use finite PBS dependencies, not a recurring reminder. Existing large-system data and figures remain unchanged. Results and job identifiers will be recorded here; notebook subsections belong under the two-dimensional CML section.

Runtime is pinned to `06b3f76875d9c487605dcb9139712e7645b71ce0` on the isolated `codex/cml2d-full-observation` history. Remote runtime worktree: `/scratch/ql44/we2614/cml2d-full-source-260914`; pyspi remains `65317c9c1fd5f12358b8ede09b7576ef001a76dd`. Physics job `178965343` uses 96 cores; export `178965344`; one-record smoke jobs `178965345` (L=6) and `178965347` (L=8). These are submitted, not completed results. Full-observation numerical tests pass locally and on Gadi (13 tests); the added coverage/resource-planning tests bring the local total to 15. Notebook has 53 executed cells with separate, explicitly pending L=6 and L=8 subsections.

Each representative node batch adds 23 development recordings (the eight development seeds at the three fixed figure anchors, minus the smoke already run). A one-shot dependent continuation examines all 24 successful timing records per size and launches only the remaining specified bank, reusing existing outputs. It requests 8 GB per concurrent recording and whole 48-core nodes; 680 workers for modest tails, or 336 for maximum/median runtime above two. Walltime is derived from the measured maximum with a margin; requirements above eight hours stop for review. Failed predecessor jobs prevent the continuation. Submitted production/report identifiers and measured plans are recorded immediately in `production-submission.json` within the result root. This is a finite dependency graph, not a scheduled reminder or repeated experiment.

Result root: `/g/data/ql44/we2614/mts-spi-data/order_parameter/cml2d_full_observation_260914`; physics masters: `/scratch/ql44/we2614/mts-spi-data/order_parameter/cml2d_full_observation_260914/physics`. The live account query before submission reported 84.30 kSU available, with Scratch at 190,147/202,000 allocated inodes and gdata at 54,178/70,000. Only this diagnostic is authorised for execution here; the older proposed M,T grid completion and large-L precision extension remain unlaunched.

Representative node jobs are `178965936` (L=6) and `178965937` (L=8), each dependent on its smoke and requesting one 48-core, 190-GB node for 23 workers. One-shot continuation `178965938` depends on both node batches. Its separate immutable controller snapshot is `/scratch/ql44/we2614/cml2d-full-controller-260914.TasYay`, sourced from `fb8bfc7`; this does not modify the pinned runtime worktree used by the data jobs. At the last check the initial physics job was queued for insufficient available CPUs; no physical or q results had completed. The original accepted figure PNG and timing manifest hashes remain unchanged after notebook rebuilding.

## Initial-condition follow-up: interpretation before results

User-authorised structural follow-up: resume L=6 independently without regenerating its data, and test whether small-size failure has a reproducible structural cause or a defensible workaround. Do not alter the original L=8 channel gate, inject observational noise, remove sites, or quietly choose a different target. The follow-up is physics only until a specific alternative is supported.

Prespecified checks: (1) classify every existing full-observation recording by microscopic recurrence at periods 1–32, constant-channel count, global-mean recurrence, and the linear stability of detected short cycles (full-state Floquet multipliers for N<=144); (2) scan L=5,6,7,8,9,10,12,16,24,32,64 at r=3.84,3.858,3.86212,3.866,3.89 with eight fresh seeds 260914201–208, unchanged g=0.2, 200k burn and a million-step disjoint Q reference; (3) for L=6,8 and r=3.84,3.86212,3.89 compare low-/high-r pre-equilibration (200k at r=3.84 or 3.89, then the standard target burn) and a longer target burn of two million steps. This gives 584 new physics cases. Odd/even neighbours test spatial commensurability as well as size; a first successful tested L is not a universal minimum-size theorem.

For new cases, compute a finite-time largest Lyapunov estimate using the exact tangent update, 5k tangent-alignment steps and 100k measurement steps with ten block summaries after the Q reference. Positive divergence is evidence for microscopic chaos; failure to repeat within 32 steps alone is not. Independently perturb saved final fields by 1e-12 and 1e-8 (random zero-mean directions, with maximum absolute perturbation equal to the specified magnitude), then repeat the standard burn/reference for the fixed seeds 260914001, 260914002 and 260914101 at the three anchors and both small sizes: 36 perturbation cases. These probes test robustness of particular trajectories, not global uniqueness of an attractor. Total new physics cases: 620. Existing-bank classification does not rerun its simulations.

Evidence supporting an alternative would require nondegenerate observations, robust physical trends and microscopic dynamics compatible with the intended collective transition, not merely a larger q–Q correlation. Candidate workarounds are neighbouring lattice geometry/size or a separately justified published control intercept. No alternate coupling, p90 size expansion beyond L=6, or outcome-selected preparation is automatically launched by this follow-up. Size/time/preparation effects and near-neutral stability estimates must remain explicit if unresolved.

No initial preparation is known a priori to maximise a representative small-lattice transition. The defensible physics-only comparison would use random starts and states pre-equilibrated at the low/high ends of the same r interval, followed by identical target-r burns and references. The existing benchmark remains fixed to random starts. Exactly spatially uniform starts are excluded as a proposed shortcut: the diffusive update preserves uniformity, reducing the model to a single logistic map rather than the intended spatially collective dynamics. Preparation-dependent sharpness would be reported as branch dependence or finite-time memory, not used to silently select favourable seeds.

Quantify candidate preparations without p90 using mean Q(r) with seed uncertainty, transition width where a monotone crossover exists, slopes normalised by the observed Q range, seed dispersion and half/block reference stability. Check the global-mean temporal structure as well: a positive absolute even/odd difference in a tiny chaotic lattice need not represent a persistent period-two collective cycle. Sharpness and finite-time preparation effects are distinct from SPI–SPI recovery; only p90 can test the latter. No additional preparation sweep has yet been launched.
