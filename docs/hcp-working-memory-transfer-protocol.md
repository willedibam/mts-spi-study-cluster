# HCP working-memory representation and transfer protocol

This protocol separates the current implementation scout from the intended multi-participant experiment. The scientific design below is declared before HCP predictive results. Final participant eligibility, family grouping and the primary duration remain pending; no cohort split or model fit is yet frozen. Operational evidence lives in `results/hcp_working_memory_audit_260912/` and the existing representation context.

## Question and scope

Can relationships among dependence measures support working-memory-load inference in new people and a different stimulus category, beyond separate dependence summaries and spectra, under a limited number of labelled source families? This is an external test of the broader interaction-organization representation hypothesis. It is not a consciousness experiment, a physical phase-transition claim, or a claim that neural models cannot represent the information.

The one-person scout has 16 continuous task blocks and 64 derived views: M32, two coordinate-only sensor selections, and central 16/8-second windows at 250 Hz. Its purposes are successful preprocessing/extraction, runtime and memory measurement, validity coverage and sensitivity to observation choices. A favourable one-person classification result is not a prerequisite for cohort inclusion or defining the main experiment. The frozen PC1 diagnostic is supplementary and uses no additional extraction grid.

## Cohort and split contract

The availability inventory contains 83 participants with two raw/config/E-Prime working-memory runs each. The metadata audit retrieved all 830 requested open files and found 1,923 complete-ten-trial blocks without marked bad-interval overlap; 74 people retained all four condition cells in both runs. These are metadata candidates, not final eligibility. Audit run-specific bad channels, bad intervals, ICA annotations, event alignment and the four load/category cells without examining any model's predictive performance. Keep an exclusion ledger with concrete reasons. The implementation participant 105923 is development-only; exclude its family from confirmation when grouping is available.

Family membership is restricted HCP information. Obtain legitimately approved grouping or an official unrelated-participant subset; do not infer relatedness from identifiers or obtain restricted metadata from public mirrors. Keep restricted mappings outside Git and publish only permissible aggregate/split information. The [current BALSA project](https://balsa.wustl.edu/project?project=HCP_YA) explains restricted access, and the [HCP access table](https://www.humanconnectome.org/study/hcp-young-adult/document/quick-reference-open-access-vs-restricted-data) identifies family membership as restricted. Open-data preprocessing can proceed before this is resolved.

Assign complete families to development or confirmation using a deterministic seeded shuffle, with approximately one third reserved for confirmation. All runs, blocks, sensor selections and window lengths from a family follow it. Source-only hyperparameter selection also uses family-separated folds. Freeze the actual split and eligible input hashes before evaluating target predictions. Do not substitute random block splitting for participant transfer.

Use two declared category-transfer directions: fit using face blocks and evaluate tool blocks from confirmation participants, then reverse. Same-category confirmation provides a diagnostic of the additional category shift. The prediction target is 0-back versus 2-back. Use coverage_a for source fitting and the primary confirmation comparison. Apply those same fitted models to coverage_b confirmation observations as a sensor-layout sensitivity check; no coverage_b development fitting or augmentation is part of the primary design. The same-category confirmation diagnostic uses coverage_a. Match all methods on participants, source labels, source/target windows and preprocessing; report actual family, participant and block counts separately.

## Representations and learners

The primary fixed statistical comparisons are full-p90 SPI–SPI z with PCA or PLS and a regularized simple readout, rich per-SPI summaries with an equally tuned readout, spectral summaries, and m+z as a complementarity control. Fit validity filtering, imputation, scaling and learned projections exclusively to source training data in every fold. The SPI catalogue remains `configs/pyspi/benchmarked_p90.yaml`, with both ordered off-diagonal directions retained in z.

The fixed-feature candidate manifest is `configs/analysis/hcp-statistical-candidates-260915.yaml`. The primary matched comparison is m-PCA, z-PCA and (m+z)-PCA with the same ridge grid; paired m-PLS/z-PLS is a supervised-projection sensitivity check. PCA preprocessing and PLS retain the established unweighted implementations. Primary ridge, logistic and neural losses use participant/class weights; all methods use participant-balanced selection and reporting. The PLS sensitivity comparison remains unweighted; no matched-loss or matched-compute claim is made. Spectra use the same 18 fixed band-ratio quantiles as the scout, and MiniRocket reuses the earlier four-C grid and three seeds.

Include MiniRocket as the conventional fixed convolutional-feature baseline. Include the existing shared temporal/interaction encoder and the InceptionTime implementation already verified on the synthetic and NeuroTycho studies. The neural architecture/training candidate manifest is now declared in `configs/analysis/hcp-neural-candidates-260915.yaml`, before HCP predictive results. It fixes the existing shared encoder and M32 InceptionTime architectures, candidate grids, ensembles/seeds, and mean-participant balanced-Brier selection. The grouped fitting adapter is implemented in `src/hcp_learning.py` and verified on random inputs; the final cohort manifest and data-loading/execution driver remain to be frozen before HCP training. Report extraction, fitting and tuning cost; do not describe unequal compute as matched compute. Any augmentation or extra within-recording exposure must be separately declared and available to the relevant competing methods; the earlier enriched NeuroTycho comparison is not automatically inherited as an HCP primary baseline.

Neural training uses mean-one sample weights giving equal total weight to each participant and then each load class within that participant. Epoch selection uses the mean of participant-level balanced Brier scores. For hyperparameter selection, pool out-of-fold predictions and then average participant scores; averaging fold means equally would overweight participants in smaller folds. Family folds are seeded independently of labels and waveforms, and a participant cannot map to multiple families. These choices are declared before HCP fitting and preserve the existing architectures, optimizers and candidate grids.

Learning curves should count independent source families, with actual participant/block counts alongside them. A compact candidate schedule is 4/8/16 source families in nested sets across three predetermined source-order seeds, provided cohort eligibility supports this. These counts and the confirmation allocation must be finalized before fitting; fewer usable families require an explicit protocol amendment rather than silent changes. Neural comparisons begin once the common cohort and preprocessing are ready, not after a favourable z result.

## Observation protocol and outcomes

Choose the primary T2000 or T4000 setting from extraction feasibility and predeclared representation-validity/stability diagnostics, not target classification accuracy. Keep sampling rate fixed at 250 Hz in this scout to avoid conflating duration with sample-lag semantics. Record the 1–100 Hz filter, 60 Hz notch, reference regression, run-specific explicitly labelled cardiac/ocular ICA removals (five in the original scout), physical sensor coordinates, standardization and window boundaries. These are approximate artifact corrections, not guarantees of purely neural signals. Whole-block and nested-window inputs are retained for provenance, not concatenated across gaps.

The primary prediction outcome is balanced accuracy at a fixed probability threshold of 0.5 under category/participant transfer; source validation selects hyperparameters, not a target-specific threshold. Report AUROC and Brier score to distinguish discrimination from threshold/calibration transfer. Aggregate at participant level. Use 2,000 paired whole-family bootstrap draws (seed 260915731) for 95% percentile intervals on scores and method differences. Resample target families jointly across all models and retain every participant in each sampled family; the estimand remains the mean participant score. These intervals are conditional on the fitted source models/cohort, do not quantify all training-set uncertainty, and are not simultaneous multiple-comparison intervals. Source-budget/order variation is reported separately rather than counted as additional independent target people. Report paired method differences, all declared directions and source budgets, and uncertainty rather than interpreting a small positive point estimate as superiority. No claim of practical deployment follows automatically from accurate classification of experimentally assigned memory load.

The secondary label-blind coordinate q is the first PCA component of source-fitted z; its sign is fixed without labels, and no component is selected for matching load. Compare with similarly fitted individual-SPI and spectral coordinates. For the one-person scout, eight fit/eight held blocks provide descriptive diagnostics only. A task association or plotted jump does not establish an order parameter, transition onset, or a uniquely SPI–SPI mechanism.

## Progression and stopping

Finish the current runtime/memory diagnosis before large extraction. The T4000 two-input job exceeded 32 GB after 2:27; the T2000 pair exceeded 16 GB after 39 minutes. The T2000 single-record profile completed all 289 SPIs in 54:26, with peak 23.709 GiB specifically reached by the backend phase-slope calculation. The unpatched T4000 profile was stopped after the cause was established; its partial checkpoints remain available. An opt-in process-local prefix-sum replacement computes the existing all-frequency-pairs PSI formula with linear temporary storage; synthetic and HCP equivalence checks pass (maximum HCP z change 1.46e-16), and completed full-catalogue smokes measured a two-worker T2000 peak of 4.648 GiB and a single-worker T4000 peak of 3.690 GiB. All 32 short scout views are complete; the remaining 31 long scout views are running. Preserve completed matrices and keep the catalogue and estimator semantics unchanged. Once a viable observation contract exists, use measured dataset-level parallelism for the cohort. Keep the study focused: no new anaesthesia/marmoset programme or additional generator grid is required for this HCP experiment. A negative HCP comparison narrows application utility while leaving the completed synthetic evidence intact.

An official public alternative to a full restricted family map is the [S900 unrelated-subject list](https://wiki.humanconnectome.org/docs/S900%20Unrelated%20Subjects%20CSV.html): its 339 identifiers overlap 29 available working-memory participants and 24 of the current conservative metadata candidates. This is an S900 subset declaration, not a current full S1200 family map; HCP later added genotype-informed Family_ID fields. Keep its provenance explicit and do not infer relations outside it or claim final family-safe confirmation before resolving the scout-family exclusion and current-release grouping.

Implementation audit: the current pyspi dispatch invokes multitaper phase-slope index without its configured band/resolution arguments; a numerical synthetic audit confirms the three configured band variants are identical. The memory repair deliberately preserves this historical behaviour. Record these coordinates as redundant full-band variants; a semantic correction would require a separate versioned amendment and assessment of existing results, not a silent change during extraction. Evidence: `results/hcp_working_memory_audit_260912/psi-dispatch-audit.json`.

Data staging proceeds before final splitting: the first batch contains the 24 metadata-qualified participants on the official S900 unrelated list, both runs each, approximately 76.12 GiB of raw/config/E-Prime/ICA objects. It is an access-independent preprocessing batch compatible with either the public-subset or broader-family-map route, not a final confirmation cohort. The 74-person metadata-qualified pool has all 148 required ICA objects available. All 48 staged runs have now passed waveform preparation and an independent archive audit: 590 clean blocks and 1,180 dependent M32 parent views. Final family grouping and the primary central-window duration remain pending.

Before any HCP prediction, the primary ridge heads were amended to use the same participant/class sample weights as the logistic and neural fits. The first two cohort waveform checks confirmed unequal usable condition counts (including four versus one block in a run). This prevents the primary readout from adopting an imbalanced class prior merely because QC retains different numbers of blocks. The paired PLS sensitivity analysis remains the standard unweighted implementation. A constant-feature test verifies a balanced 0.5 prior for the primary ridge model.

A metadata audit found one identical condition sequence per run across all 24 staged participants. Category swaps at every position between runs, but load changes only at global block IDs 1, 10, 13 and 22; the other 12 positions retain their load. This is a potential schedule-associated physiological shortcut, not evidence that any waveform learner actually uses one. Keep the main participant/category-transfer comparison, and report a source-trained block-position lookup as a metadata-only diagnostic. Also report all methods on the same prespecified target subset at the four discordant positions, retaining participants with both target classes there and disclosing exclusions. These analyses reuse the main predictions; no additional waveform fits or target-selected threshold is introduced. Success there challenges the simplest shared-position template, not every physiological confound or a causal interpretation. The control has different information access and is not a matched-input competitor. Settings are in `configs/analysis/hcp-evaluation-260915.yaml`, with evidence in `cohort-stage/order-audit.json`.

Canonicalize source records and reuse fits when source-order seeds yield the same source set, CV partition and model seed/configuration. In particular, a maximum budget equal to the entire development cohort produces one unique source cohort, not three independent replications. Report unique source sets and preserve genuine model-seed or fold differences explicitly.

The cohort input builder `scripts/prepare_hcp_cohort_short.py` requires an explicit `--length 2000` or `4000`, verifies each parent against the independent cohort audit, and reproduces the frozen scout operation order: notch the complete corrected parent, crop centrally, then standardize each channel. Both sensor layouts retain participant/run/block identifiers and remain dependent views. Source and output hashes guard reuse; complete archive/config/manifest sets publish together. Three tests verify numerical equivalence to the scout transformation at both lengths, separate block boundaries, reuse and rejection of changed inputs/configurations. Cohort window construction has not yet run; this implementation does not itself choose the primary duration.

Extraction follows the actual evaluation inputs: prepare both layout waveforms for provenance, but first compute coverage_a features for the 590 eligible cohort blocks. After the family-separated split is frozen, compute coverage_b only for confirmation participants used in the layout sensitivity check. This avoids extracting unused development-layout features and does not drop any declared comparison. At the September 15 capacity check, gdata had 66,655/70,000 inodes used despite only 29.34/100 GiB storage used. Each completed external-runner record requires two files and one directory; one layout first requires roughly 1,770 output inodes, versus 3,540 for both layouts of every block. Recheck headroom before submission and preserve a margin for other workstreams.
